import { Component } from 'react'

/**
 * Formulario para importar un CSV
 * Leer los datos del Excel y enviarlos en JSON al otro componente
 */
class Home extends Component {
  render () {
    return <div>
      Esta es la raiz de la aplicacion
    </div>
  }
}

export default Home